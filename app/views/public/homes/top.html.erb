<div class="container-fluid">
  <div class="row">
    <div class="col-2 ml-3">
      <div class="border border-light p-1 category">
        <h6><i class="fas fa-birthday-cake"></i> ジャンル検索</h6>
      </div>
      <div class="border border-light p-3", style="background-color:#e6e6fa;">
        <ul class="list-unstyled">
          <% @categories.each do |category| %>
          <li class="categorymenu"><i class="fas fa-cookie"></i> <%= category.name %></li>
          <% end %>

        </ul>
      </div>
    </div>
    <div class="col-9 mx-5">
      <div class="bg", style="padding-top:10px; background-color:#000;">
      <div class="col-8 mx-auto introduction">
        <p>
          ようこそ、ながのCAKEへ！<br>
          このサイトは、ケーキ販売のECサイトになりますが。<br>
          会員でない方も商品の閲覧はできますが、<br>
          購入には会員登録が必要になります。
        </p>
      </div>
      </div>
      <h4 style="display:inline-block;">新着商品</h4>
      <div class="d-flex flex-row mt-2">
        <% @products.each do |product| %>
        <div class="d-flex flex-column mr-3">
          <div>
            <%= link_to product_path(product) do %>
              <%= attachment_image_tag product, :image, :fill, 200, 200, fallback: "no_image.jpg" %>
            <% end %>
          </div>
          <div style="font-size:20px;"><%= product.name %></div>
          <div>¥ <%= product.price.to_s(:delimited) %></div>
        </div>
        <% end %>
      </div>

    </div>
  </div>
</div>